$schema: https://git.drupalcode.org/project/drupal/-/raw/10.1.x/core/modules/sdc/src/metadata.schema.json
name: off-canvas dialog
description: Using the popover-api component to create an off-canvas dialog

libraryOverrides:
  dependencies:
    - core/drupal.dialog.off_canvas

slots:
  contents: { }

props:
  type: object
  additionalProperties: false
  properties:
    this:
      type: string
      title: This
    that:
      type: string
      title: That
